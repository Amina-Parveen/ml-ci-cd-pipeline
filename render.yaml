Services:
  - type: web
    name: ml-flask-api
    Env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --bind 0.0.0.0:$port"
    autoDeploy: fasle   #we'll trigger deploys via Github Actions
